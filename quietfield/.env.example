# Quietfield environment
# Copy to .env and fill in from Supabase Project Settings > API.
# The anon key is safe for the frontend; RLS policies protect the data.
# The service key must NEVER appear here or anywhere in frontend code.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
